# 소비 전력 실험실 — Electricity

## ① 앱의 목적과 대상 단원

- **목적** : 중학교 3학년 과학 「에너지 전환과 보존」의 **전기 에너지 전환과 소비 전력**을
  ① 전기 에너지가 쌓이는 것을 보는 **실험실**, ② 예측 → 확인의 **미션 게임**,
  ③ 웹에서 작성해 PDF 로 제출하는 **학습지**로 익히게 한다.
- **대상** : 중학교 3학년 · 과학
- **관련 성취기준** (2015 개정)

  | 기준 | 내용 | 여기서 담당하는 화면 |
  |---|---|---|
  | **[9과22-03]** | 가정에서 전기 에너지가 다양한 형태의 에너지로 전환되는 예를 들고, 이를 소비 전력과 관련지어 설명할 수 있다. | 💡 소비 전력 · 🔌 정격 전압 · 🏠 우리 집 전력량 |
  | [9과22-02] | 자석의 운동으로 전기가 만들어진다 *(앞 소단원)* | `electromagnetic-induction` 앱 |

### 출처 — 선생님의 실제 수업 자료에서 범위를 잡았다

```
C:\김미란(2023.11.20)\학습자료(2023.11.20)\2026. 3년 과학(2015)\6단원.hwpx
  Ⅵ. 에너지 전환과 보존
    1. 여러 가지 에너지 전환과 보존              [9과22-01]  ← EnergyKeeper
    2. 전기 에너지 발생과 이용
       (1) 자석의 운동으로 전기를 만들 수 있어   [9과22-02]  ← electromagnetic-induction
       (2) 전기 에너지 전환과 소비 전력은? p242~245 [9과22-03] ← **이 앱**
```

이 학습지의 물음이 그대로 이 앱의 뼈대다. **숫자를 하나도 바꾸지 않았다.**

| 선생님 학습지의 물음 | 답 | 이 앱에서 |
|---|---|---|
| 정격전압 / 소비전력의 뜻 | 알맞은 전압(V) / 1초 동안 쓰는 전기 에너지(W) | 학습지 1·5번 |
| 전열기가 1초 동안 / 10초 동안 | **1200 J** / **12000 J** | 💡 1장면 · 학습지 3·4번 |
| 전열기를 110V 에 연결해도 1초에 1200J? | **아니다** (300 W, 4분의 1) | 🔌 2장면 · 학습지 6·7번 · 미션 4 |
| 선풍기(44W)가 1초 동안 | **44 J** | 학습지 9번 · 미션 1 |
| 전기다리미(1075W)가 10초 동안 | **10750 J** | 학습지 10번 · 미션 2 |
| 스탠드(7W)가 140J 을 썼다. 시간은? | **20초** | 학습지 11번 · 미션 3 |
| 한 시간 동안 가장 많이 쓰는 것 | **전기다리미** | 학습지 12번 |
| 헤어드라이어(1760W) 10분 = 선풍기 얼마? | **400분(6시간 40분)** | 학습지 13번 · 미션 6 |
| 전력량(Wh) = 소비 전력(W) × 시간(h) | — | 🏠 3장면 · 학습지 14번 |
| 가정용 표의 전력량 계산 | 180 / 400 / 500 / 1600 / 12 Wh | 학습지 15번(표) · 미션 5 |

> ⚠ **가전제품 숫자를 '더 실제 같게' 고치지 말 것.**
> 스피커 800W 처럼 실제보다 큰 값도 있지만, 학생이 **종이와 화면을 나란히 놓고 본다.**

### 왜 이렇게 만들었나 (설계 의도)

**이 단원에서 학생이 막히는 곳은 W · J · Wh 세 단위가 따로 논다고 느끼는 것이다.**

그래서 이 앱은 **한 문장으로 셋을 묶었다** — <b>W 는 '1초에 몇 J'</b>.

1. **1초마다 에너지 덩어리가 쌓이는 것을 눈금으로 보여 준다.**
   네모 한 칸이 1초 동안 쓴 J 이다. 시간을 밀면 칸이 늘어난다.
   "E = P × t" 를 외우기 전에 **쌓이는 양으로** 먼저 보게 하려는 것이다.
2. **J 과 Wh 는 다른 양이 아니다.** 계기판에 늘 둘을 같이 띄운다(1 Wh = 3600 J).
3. **미션 5 가 이 단원의 진짜 결론이다.** 소비 전력이 가장 큰 것은 다리미(1000 W)인데
   하루 전력량이 가장 큰 것은 **스피커**(1600 Wh)다.
   **센 것과 많이 쓰는 것은 다르다** — 전기 요금이 전력량으로 매겨지는 이유다.

## ② 기능 목록

### 파일 구성

| 파일 | 설명 |
|---|---|
| `index.html` | **실험실.** 장면 3개 + 미션 6개 + 계기판 |
| `worksheet.html` | **학습지.** 23문항(자동 채점 19) → PDF 저장 → 잠금 → 정답 공개 · 인쇄 가능 |
| `teacher.html` | **선생님용 제출물 정리** |
| `js/power.js` | **계산 엔진.** 전기 에너지·전력량·정격 전압. 화면을 전혀 모른다 |
| `js/lab.js` | 실험실 화면 |
| `js/pdf.js` | 공용 PDF 엔진 (**다섯 앱이 같은 파일**) |
| `css/app.css` | 공용 스타일 + 인쇄 스타일 (**다섯 앱이 같은 내용**) |
| `server.py` | 로컬 서버 (**8600번대** 포트) |
| `로컬서버_실행.bat` | 더블클릭 실행. **CP949(ANSI) + CRLF** |

**포트 8600** — binary-converter 8000 · ai-class 8100 · EnergyKeeper 8200 ·
electromagnetic-induction 8300 · brightness 8400 · parallax 8500 다음이다.

### 실행 방법

`index.html` 더블클릭. 외부 의존성 0개 · npm 없음 · 빌드 없음.
(선생님 PC 에는 파이썬이 없으므로 **더블클릭이 정답**이다.)

### 💡 실험실

| 장면 | 무엇을 보나 | 조작 |
|---|---|---|
| 💡 소비 전력 | **1초마다 네모 한 칸**씩 에너지가 쌓인다 | 기구 · 사용 시간 |
| 🔌 정격 전압 | 전압 막대와 전력 막대를 나란히 — **제곱**으로 줄어든다 | 기구 · 전압 |
| 🏠 우리 집 전력량 | 선생님 표를 가로 막대로 — **소비 전력 순서와 전력량 순서가 다르다** | 가전제품 · 사용 시간 |
| 🏆 미션 | 예측 → 확인 (6단계) | 미션마다 다름 |

애니메이션이 없어 `requestAnimationFrame` 을 돌리지 않는다.

### 🏆 미션 6개

| # | 제목 | 정답 | 알게 하는 것 |
|---|---|---|---|
| 1 | 💡 44 W 는 무슨 뜻일까 | 44 J | **W = 1초에 몇 J** |
| 2 | ⏱️ 10초 동안이라면 | 10750 J | E = P × t |
| 3 | 🔍 거꾸로 물으면 | 20초 | t = E ÷ P |
| 4 | 🔌 110 V 에 꽂으면 | 300 W (1/4) | **제곱**으로 줄어든다 |
| 5 | 🏠 전력량이 가장 큰 것은? | 스피커 | **센 것 ≠ 많이 쓰는 것** |
| 6 | 💨 헤어드라이어 10분 | 6시간 40분 | 전력이 40배면 시간은 1/40 |

⚠ **미션의 `setup` 은 정답 자리에서 시작하면 안 된다.** 미션 1·6 이 처음에 그렇게 되어
시작하자마자 깨졌다(검증에서 발견). 지금은 일부러 다른 값에서 시작한다.

### 📝 학습지

- **23문항 · 자동 채점 19문항** (choice 9 · num 7 · ox 3), 나머지는 서술 3 · 표 1.
- 5단계 : ① 소비 전력이란 ② 정격 전압이 아니면 ③ 가전제품의 소비 전력 ④ 전력량 ⑤ 에너지 전환과 정리
- **PDF 5쪽** · 파일 이름 `소비전력학습지_3-4-12_홍길동_20260814_2041.pdf`
- ⚠ 숫자 문항 7개의 답은 엔진과 맞물려 있다(1200 · 12000 · 44 · 10750 · 20 · 400 · 3600).

### 🖨️ 인쇄

`Ctrl+P` → **A4 두 쪽**(본문 472 mm · 채움 96% / 80%).
이 학습지만 **9.5 pt**. 공용값(9pt)으로는 465mm 라 둘째 쪽이 74% 밖에 안 찼고,
10 pt 까지 올리면 세 쪽으로 넘어간다.

| 앱 | 문항 | 인쇄 글자 | 본문 | 채움 |
|---|---|---|---|---|
| parallax | 24 | 9 pt (공용) | 479 mm | 96% / 82% |
| **Electricity** | **23** | **9.5 pt** | **472 mm** | **96% / 80%** |
| electromagnetic-induction | 22 | 9 pt (공용) | 488 mm | 98% / 84% |
| brightness | 21 | 10 pt | 502 mm | 97% / 90% |
| EnergyKeeper | 15 | 10.5 pt | 467 mm | 83% / 91% |

## 엔진의 핵심 아이디어 (`js/power.js`)

### 1. **W 는 '1초에 몇 J'** 한 문장으로 모든 계산을 푼다

함수는 셋뿐이고 셋이 모두 같은 한 줄에서 나온다.

```
전기 에너지 E(J)  = P(W) × 시간(초)
전력량     E(Wh) = P(W) × 시간(시간)
시간              = E ÷ P            ← 거꾸로 묻는 문항
```

**J 과 Wh 는 다른 양이 아니다** — 시간을 초로 쟀느냐 시간으로 쟀느냐의 차이뿐이고,
`1 Wh = 3600 J` 이 둘을 이어 준다.

### 2. 정격 전압이 아니면 전력이 **제곱으로** 줄어든다

기구의 저항 R 은 그대로이므로 `R = V₀² ÷ P₀`, 따라서

```
P = V² ÷ R = P₀ × (V ÷ V₀)²
```

전압이 절반이면 전력은 **(1/2)² = 1/4**. 이 **제곱**이 2장면의 결론이다.

⚠ 실제 전열기는 온도가 오르면 저항이 조금 변하지만,
**중학교 범위에서는 저항을 일정하다고 본다.** 그래야 위 식이 그대로 성립한다.

### 3. 가전제품 값은 **선생님 학습지 그대로**

`APPLIANCES` 는 소비전력 표(스탠드 7 · 선풍기 44 · 전기다리미 1075 · 헤어드라이어 1760 …),
`HOME` 은 가정용 표(전등 30W 6h · 선풍기 40W 10h · 다리미 1000W 0.5h · 스피커 800W 2h ·
배터리충전기 6W 2h). **표를 둘로 나눈 것도 종이가 그렇기 때문이다.**

## ③ 현재 개발 상태와 다음 할 일

### 상태 : 1차 완성 — 3개 페이지 + 계산 엔진, 브라우저 검증 통과 (2026-08-14)

### 계산 정확도 검증 — **선생님 학습지의 답과 하나씩 맞춰 보았다**

| 학습지 물음 | 엔진이 낸 값 |
|---|---|
| 전열기 1초 / 10초 | **1200 J** / **12000 J** |
| 선풍기 1초 | **44 J** |
| 전기다리미 10초 | **10750 J** |
| 스탠드가 140 J 을 쓴 시간 | **20 초** |
| 헤어드라이어 10분 | **1,056,000 J** → 선풍기 **24,000초 = 6시간 40분** |
| 전열기를 110 V 에 | **300 W** (정격의 **0.2500** 배 = 정확히 1/4) · 저항 40.33 Ω |
| 가정용 표 전력량 | 180 / 400 / **500** / **1600** / 12 Wh · 합계 **2692 Wh = 2.692 kWh** |
| **전력량 순서** | 스피커 > 다리미 > 선풍기 > 전등 > 충전기 |
| **소비 전력 순서** | **다리미** > 스피커 > 선풍기 > 전등 > 충전기 |
| 1 Wh ↔ J | **3600 J** ↔ 1 Wh |

**전력량 순서와 소비 전력 순서의 1·2위가 뒤바뀌는 것**이 미션 5 의 근거다.

### 미션 검증

여섯 미션 모두 **⬜ → ✅ → `won`**, 진행도 **6 / 6**.
미션 1·6 은 처음에 `setup` 이 정답 자리라 **시작하자마자 깨지는 문제**가 있었고, 고친 뒤 재검증했다.

### 화면·PDF·인쇄 검증

| 검증 내용 | 결과 |
|---|---|
| 세 장면 그리기 | 소비 전력 252 · 정격 전압 261 · 우리 집 417 가지 색 |
| **가장자리 넘침** | 가전제품 5종 × 24시간 · 헤어드라이어 600초 · 55 V — **모두 0** (처음엔 우리 집 장면에서 넘쳤고, 값 글자를 오른쪽 정렬하고 막대 폭을 줄여 고쳤다) |
| 학습지 문항 수 | **23문항** · 자동 채점 **19** (choice 9 · num 7 · ox 3) |
| 학습지 채점 | 0/19 → 만점 **19/19** · 허용 오차 안은 정답, 5배는 오답 |
| 학습지 PDF | **5쪽 · 1.00 MB** · JPEG 5개 · `%PDF-1.4` · **오프셋 일치** |
| 제출 잠금 | 입력 **63개 전부 잠김** · `자동 채점 19 / 19` 띠 |
| **인쇄 쪽수** | 본문 **472 mm** · 여백과 `break-after: avoid` 반영해 **2쪽** (96% / 80%) |
| 콘솔 오류 | 실험실·학습지 **없음** |
| `file://` 전제 | 모듈 · `import` · `fetch` · 외부 CDN · `localStorage` **0건** |
| `.bat` 인코딩 | CP949 정상(UTF-8 로 읽으면 깨짐) · BOM 없음 · CRLF = LF · `chcp` 없음 |

### 아직 검증하지 못한 것

- [ ] 실제 브라우저에서 **PDF 다운로드가 파일로 떨어지는지** 1회 확인
- [ ] **인쇄본을 실제 프린터로** 한 장 뽑아 보기 (개발 환경이 화면을 못 그려 눈으로는 못 봤다)
- [ ] `index.html` **더블클릭(`file://`) 실행** 1회 확인
- [ ] **태블릿 터치 조작**
- [ ] `server.py` 실행 (이 PC 에 파이썬이 없다)

### 다음 할 일

- [x] 계산 엔진 + 실험실 3장면 + 미션 6개 (2026-08-14)
- [x] 학습지 23문항 + PDF 제출 + 인쇄(A4 2쪽) (2026-08-14)
- [x] 교사용 제출물 정리 (2026-08-14)
- [ ] **선생님이 학습지 내용을 검토**
- [ ] **GitHub Pages 켜기** — Settings → Pages → Branch `main` / `/ (root)`
- [ ] **전기 요금 계산 화면** — 전력량(kWh)에 요금 단가를 곱하면 생활과 더 붙는다.
      다만 요금 체계(누진제)가 복잡하니 넣을지 판단 필요.
- [ ] **에너지 소비 효율 등급** — 같은 일을 하면서 전력이 적은 기구를 고르는 이야기.

## 코드 구조 메모

- **미션을 추가할 때** : `js/lab.js` 의 `MISSIONS` 에 항목만. 판정은 `checkGoal(key)` 에 분기 하나.
  ⚠ **`setup` 이 정답 자리면 안 된다**(미션 1·6 에서 실제로 겪었다).
- **`pickMission()` 은 `S.scene = "mission"` 을 먼저 맞춘다**(앞선 앱들의 함정).
- **가전제품을 추가할 때** : `power.js` 의 `APPLIANCES` 또는 `HOME` 에 항목만 넣는다.
  **선생님 학습지에 있는 숫자는 고치지 말 것.**
- **우리 집 장면의 사용 시간은 학생이 바꾼다** → `lab.js` 가 `W.HOME` 을 **복사해서** 들고 있다.
  원본을 직접 고치면 '처음으로' 가 동작하지 않는다.
- **`ElLab._test`** 는 검사용 손잡이(`set` `scene` `pick` `answer` `goals` `state` `draw`).
  `ELSheet` 는 학습지 쪽(`score` `makePdf` `forceSubmit`).

## 이 개발 환경에서 검증할 때

- 작업 폴더 밖의 `file://` 은 정적 스냅샷 → 로컬 서버로 `http://` 로 열 것.
- 이 PC 에는 파이썬도 node 도 없다. PowerShell `HttpListener` 임시 서버를 쓴다.
  ⚠ `.ps1` 은 **BOM 있는 UTF-8** 로 저장할 것.
- **미리보기가 화면을 못 그릴 때가 있다**(스크린샷 실패) → **수치로** 확인한다.
  이 앱의 '가장자리 넘침' 검사처럼 `getImageData` 로 픽셀을 세면 눈 없이도 확인된다.
- 같은 URL 로 `navigate` 하면 새로 로드되지 않는다 → `?v=2` 로 쿼리를 바꿀 것.
- ⚠ `String.fromCharCode(...buf)` 금지 · `lastIndexOf('xref')` 금지(`startxref` 에 걸림).

## 🔑 정답지 (2026-08-16 추가)

`worksheet.html?key` — 학생용 학습지와 **같은 SHEET 를 그대로 읽어** 정답만 뽑아 준다.
`teacher.html` 위쪽의 **🔑 정답지** 링크로 들어간다.

- **PDF 를 만들지 않아도** 정답이 보인다. 종이 학습지를 채점할 때 쓰라고 만든 것이다.
- 기본은 **정답만**. 「문제도 함께」·「해설도 함께」를 켜면 늘어난다.
- 정답을 따로 적어 두지 않으므로 **문항을 고치면 정답지도 저절로 따라온다.**
- 예외는 **표(`tbl`)** 뿐이다. 표는 정답이 자료에 없어 문항에 `keyAns` 를 함께 적어 둔다.
  배열이면 줄마다 값을 늘어놓고, **문자열이면 그대로** 쓴다(정답이 하나로 정해지지 않는 표).
- 서술형(`text`)은 정답 자리에 `why`(예시 답안)를 넣고, 해설을 두 번 쓰지 않는다.

| 인쇄 | 본문 | 쪽수 |
|---|---|---|
| **정답만** (기본) | 197.5 mm | **1쪽** |
| 문제 포함 | 280.0 mm | 2쪽 |
| 문제 + 해설 포함 | 407.0 mm | 2쪽 |

⚠ **가리는 장치가 아니다.** 정답은 원래부터 `worksheet.html` 안의 자료에 들어 있어
소스를 보면 어차피 보인다. 학생이 주소를 치면 열리므로, 학생에게 주소를 알려 주지 않는 것으로만 막힌다.

⚠ 정답지 화면(`#keyRoot`)과 스타일(`.k*` · `body.keymode`)은 **일곱 앱이 모두 같다.**
`css/app.css` 맨 끝의 정답지 블록은 앱마다 **글자 하나까지 같게** 유지한다.
